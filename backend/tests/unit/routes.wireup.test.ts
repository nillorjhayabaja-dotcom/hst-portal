/*
 * Smoke tests for route wiring.
 *
 * NOTE:
 * The current backend uses TanStack Start/Vite tooling for its frontend and
 * Vitest startup is failing with a Vite "Could not resolve entry for router" error.
 * Keeping this file for future enablement once the test runner config is fixed.
 */

// Intentionally no exports and no test suites.
// Vitest will error on "no test suite found" if this file is included.
// Keeping the file here only as documentation; ensure vitest include/exclude
// does not match this file by name in vitest.config.ts.
