import { BaseRepository } from './base.repository';

export class DelegationRepository extends BaseRepository<any> {
  async getActiveByUser(userId: string) {
    // Implementation coming in Sprint 4
    return [];
  }
}
