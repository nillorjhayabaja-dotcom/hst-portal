import { BaseRepository } from './base.repository';

export class MRFRepository extends BaseRepository<any> {
  async getByDepartment(departmentId: string) {
    // Implementation coming in Sprint 10
    return [];
  }
}
